# Isolated unit test for the BLE connection registry
# (src/meshtastic_ble_registry.c). The module is pure C with no Bluetooth or
# kernel dependency, so nothing beyond ztest is needed; without CONFIG_BT the
# registry sizes itself to its unit-test fallback capacity (4 slots).
CONFIG_ZTEST=y
