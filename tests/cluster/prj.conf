# Isolated unit test for the cluster document (src/meshtastic_cluster_doc.c):
# table + LWW merge + digest hash + effective(). Nothing beyond ztest and the
# CRC helper is needed.
CONFIG_ZTEST=y
CONFIG_CRC=y
