# Isolated unit test for the BLE peer-link wire formats and rx accounting
# (src/meshtastic_ble_peer_codec.c). Pure C with no Bluetooth or kernel
# dependency; nothing beyond ztest is needed.
CONFIG_ZTEST=y
