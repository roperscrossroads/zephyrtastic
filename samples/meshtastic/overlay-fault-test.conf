# TEST ONLY — a deliberately bad image for proving the revert path (F5,
# DECLARATIVE-FLEET.md §8 R4). Stack it on a normal target build
# (…;overlay-ota-ble.conf;overlay-fault-test.conf) with a NEWER version than the
# node runs, load it into a courier's depot, desire it: the target swaps, beats
# the new version, faults at this many seconds, reboots, MCUboot reverts it, and
# the courier must show REVERTED and never push that version again.
# Never ship an image built with this.
CONFIG_MESHTASTIC_OTA_FAULT_TEST_SEC=45
