# Verification overlay — overflow guard + on-demand stack high-water marks.
CONFIG_STACK_SENTINEL=y
# Enables the `kernel stacks` shell command (per-thread used/size high-water marks).
CONFIG_THREAD_MONITOR=y
CONFIG_INIT_STACKS=y
